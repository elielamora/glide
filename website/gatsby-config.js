/** @type {import('gatsby').GatsbyConfig} */
module.exports = {
  siteMetadata: {
    title: "Glide",
    description:
      "The readable Lisp that compiles to Go — expressive syntax, zero-cost abstractions, and Go's concurrency model.",
    author: "@elielamora",
    siteUrl: "https://glide.dev",
  },
  plugins: ["gatsby-plugin-postcss"],
};
